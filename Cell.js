export class Cell {
    constructor(x, y, state) {
        this.x = x;
        this.y = y;
        this.state = state;
    }

    toString() {
        return
    }

    static hello(x) {
        return "Hello ";
    }
}